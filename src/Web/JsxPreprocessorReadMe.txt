npx babel --watch <source> --out-dir <destination> --presets react-app/prod

npx babel --watch src --out-dir . --presets react-app/prod
src folder to the project root path

npx babel --watch src --out-dir wwwroot/js --presets react-app/prod
src folder to wwwroot/js static files folder